// Package wyoming implements the Wyoming wire protocol — a JSON-header +
// optional binary-payload TCP protocol used by local voice services
// (STT, wakeword, TTS), originated by Home Assistant.
//
// This package keeps zero dicta-specific imports so it stays promotion-ready
// as a standalone module (D16).
package wyoming
